# ttyd with the stock OpenSSH client

This image keeps the upstream ttyd server unchanged and adds only the standard
OpenSSH client required for ttyd to connect to the workspace container over
localhost. It does not implement a terminal or SSH protocol.

